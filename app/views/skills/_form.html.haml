= form_for @skill do |f|
  - if @skill.errors.any?
    #error_explanation
      %h2
        = "#{pluralize(@skill.errors.count, "error")} prohibited this role from being save:"
      %ul
        - @skill.error.full_messages.each do |message|
          %li
            = message
    
  .form-group
    = f.label 'Nombre'
    = f.text_field :name, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-success btn-block text-white'